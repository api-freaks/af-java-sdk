/**
 * This file was auto-generated by Fern from our API Definition.
 */

package types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import core.ObjectMappers;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = OcrPredictResponse.Builder.class
)
public final class OcrPredictResponse {
  private final OcrPredictResponseOcrText ocrText;

  private final Map<String, Object> additionalProperties;

  private OcrPredictResponse(OcrPredictResponseOcrText ocrText,
      Map<String, Object> additionalProperties) {
    this.ocrText = ocrText;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return Array containing extracted text. Structure varies based on input type and new_line parameter:
   * <ul>
   * <li>Single file, new_line=0: Array with single string element</li>
   * <li>Single file, new_line=1: Array of strings (one per line)</li>
   * <li>Bulk/ZIP file, new_line=0: Array of strings (one per file)</li>
   * <li>Bulk/ZIP file, new_line=1: Array of arrays (each inner array contains lines for respective file)</li>
   * </ul>
   */
  @JsonProperty("OCRText")
  public OcrPredictResponseOcrText getOcrText() {
    return ocrText;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof OcrPredictResponse && equalTo((OcrPredictResponse) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(OcrPredictResponse other) {
    return ocrText.equals(other.ocrText);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.ocrText);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static OcrTextStage builder() {
    return new Builder();
  }

  public interface OcrTextStage {
    /**
     * <p>Array containing extracted text. Structure varies based on input type and new_line parameter:</p>
     * <ul>
     * <li>Single file, new_line=0: Array with single string element</li>
     * <li>Single file, new_line=1: Array of strings (one per line)</li>
     * <li>Bulk/ZIP file, new_line=0: Array of strings (one per file)</li>
     * <li>Bulk/ZIP file, new_line=1: Array of arrays (each inner array contains lines for respective file)</li>
     * </ul>
     */
    _FinalStage ocrText(@NotNull OcrPredictResponseOcrText ocrText);

    Builder from(OcrPredictResponse other);
  }

  public interface _FinalStage {
    OcrPredictResponse build();

    _FinalStage additionalProperty(String key, Object value);

    _FinalStage additionalProperties(Map<String, Object> additionalProperties);
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder implements OcrTextStage, _FinalStage {
    private OcrPredictResponseOcrText ocrText;

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    @java.lang.Override
    public Builder from(OcrPredictResponse other) {
      ocrText(other.getOcrText());
      return this;
    }

    /**
     * <p>Array containing extracted text. Structure varies based on input type and new_line parameter:</p>
     * <ul>
     * <li>Single file, new_line=0: Array with single string element</li>
     * <li>Single file, new_line=1: Array of strings (one per line)</li>
     * <li>Bulk/ZIP file, new_line=0: Array of strings (one per file)</li>
     * <li>Bulk/ZIP file, new_line=1: Array of arrays (each inner array contains lines for respective file)</li>
     * </ul>
     * <p>Array containing extracted text. Structure varies based on input type and new_line parameter:</p>
     * <ul>
     * <li>Single file, new_line=0: Array with single string element</li>
     * <li>Single file, new_line=1: Array of strings (one per line)</li>
     * <li>Bulk/ZIP file, new_line=0: Array of strings (one per file)</li>
     * <li>Bulk/ZIP file, new_line=1: Array of arrays (each inner array contains lines for respective file)</li>
     * </ul>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("OCRText")
    public _FinalStage ocrText(@NotNull OcrPredictResponseOcrText ocrText) {
      this.ocrText = Objects.requireNonNull(ocrText, "ocrText must not be null");
      return this;
    }

    @java.lang.Override
    public OcrPredictResponse build() {
      return new OcrPredictResponse(ocrText, additionalProperties);
    }

    @java.lang.Override
    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    @java.lang.Override
    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
