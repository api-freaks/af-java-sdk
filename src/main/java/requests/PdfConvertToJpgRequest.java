/**
 * This file was auto-generated by Fern from our API Definition.
 */

package requests;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import core.ObjectMappers;
import java.lang.Boolean;
import java.lang.Integer;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;
import types.PdfConvertToJpgRequestFormat;
import types.PdfConvertToJpgRequestProfile;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = PdfConvertToJpgRequest.Builder.class
)
public final class PdfConvertToJpgRequest {
  private final Optional<String> webhookAuthorization;

  private final String apiKey;

  private final Optional<PdfConvertToJpgRequestFormat> format;

  private final Optional<String> fileId;

  private final Optional<Boolean> destroy;

  private final Optional<String> output;

  private final Optional<Integer> quality;

  private final Optional<String> pages;

  private final Optional<Integer> resolution;

  private final Optional<String> imageSmoothing;

  private final Optional<PdfConvertToJpgRequestProfile> profile;

  private final Optional<String> webhookUrl;

  private final Optional<Boolean> webhookFailureNotification;

  private final Map<String, Object> additionalProperties;

  private PdfConvertToJpgRequest(Optional<String> webhookAuthorization, String apiKey,
      Optional<PdfConvertToJpgRequestFormat> format, Optional<String> fileId,
      Optional<Boolean> destroy, Optional<String> output, Optional<Integer> quality,
      Optional<String> pages, Optional<Integer> resolution, Optional<String> imageSmoothing,
      Optional<PdfConvertToJpgRequestProfile> profile, Optional<String> webhookUrl,
      Optional<Boolean> webhookFailureNotification, Map<String, Object> additionalProperties) {
    this.webhookAuthorization = webhookAuthorization;
    this.apiKey = apiKey;
    this.format = format;
    this.fileId = fileId;
    this.destroy = destroy;
    this.output = output;
    this.quality = quality;
    this.pages = pages;
    this.resolution = resolution;
    this.imageSmoothing = imageSmoothing;
    this.profile = profile;
    this.webhookUrl = webhookUrl;
    this.webhookFailureNotification = webhookFailureNotification;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return Optional custom header for webhook requests. Format: <code>Key:Value</code> (e.g., <code>Authorization:Bearer token123</code>). This will be sent as an HTTP header in the webhook call.
   */
  @JsonIgnore
  public Optional<String> getWebhookAuthorization() {
    return webhookAuthorization;
  }

  /**
   * @return Your API key
   */
  @JsonProperty("apiKey")
  public String getApiKey() {
    return apiKey;
  }

  /**
   * @return Specifies the desired format for the API response. Choose 'json' for a JSON object or 'xml' for an XML structure.
   */
  @JsonProperty("format")
  public Optional<PdfConvertToJpgRequestFormat> getFormat() {
    return format;
  }

  /**
   * @return The unique ID of a PDF file already uploaded to the API Freaks server. Use this as an alternative to uploading a new file directly.
   */
  @JsonProperty("file_id")
  public Optional<String> getFileId() {
    return fileId;
  }

  /**
   * @return If set to <code>true</code>, the input file(s) will be permanently deleted from the server immediately after the output PDF is generated.
   */
  @JsonProperty("destroy")
  public Optional<Boolean> getDestroy() {
    return destroy;
  }

  /**
   * @return The desired name for the output unrestricted PDF file. If not provided, a default name will be assigned.
   */
  @JsonProperty("output")
  public Optional<String> getOutput() {
    return output;
  }

  /**
   * @return Controls JPG compression quality. Higher values yield sharper images with larger file sizes.
   */
  @JsonProperty("quality")
  public Optional<Integer> getQuality() {
    return quality;
  }

  /**
   * @return Specifies the pages or ranges at which to split the PDF. Accepts individual page numbers (e.g., '1') and/or page ranges (e.g., '4-2', 'last'). Ranges can be ascending or descending. Use commas to separate entries and hyphens for ranges. Alternatively, provide only one of the following keywords: 'even' (split at every even-numbered page), 'odd' (split at every odd-numbered page), 'last' (split at the last page only), or 'all' (split into single pages). Examples: '1,4-2,last', 'odd', 'all'. Mixing special keywords with specific pages/ranges is not allowed.
   */
  @JsonProperty("pages")
  public Optional<String> getPages() {
    return pages;
  }

  /**
   * @return Specifies the resolution (in DPI) for the output images. Acceptable Range is from 20 to 1200.
   */
  @JsonProperty("resolution")
  public Optional<Integer> getResolution() {
    return resolution;
  }

  /**
   * @return Determines the smoothing options to apply during image conversion. Valid values are 'none', 'all' or a combination of 'text', 'line', and 'image' (comma-separated).If not provided, no smoothing will be applied.
   */
  @JsonProperty("image_smoothing")
  public Optional<String> getImageSmoothing() {
    return imageSmoothing;
  }

  /**
   * @return Specifies the color profile for the output PNG images. Acceptable values: bw (1-bit black &amp; white, smallest size, no grayscale or color), gray (8-bit grayscale), rgb (24-bit RGB color, default), rgba (32-bit RGB color with alpha channel for transparency), 4-bit (4-bit indexed color, up to 16 colors, smaller size), or 8-bit (8-bit indexed color, up to 256 colors).
   */
  @JsonProperty("profile")
  public Optional<PdfConvertToJpgRequestProfile> getProfile() {
    return profile;
  }

  /**
   * @return The URL to which the webhook notification will be sent after the task is completed.
   */
  @JsonProperty("webhook_url")
  public Optional<String> getWebhookUrl() {
    return webhookUrl;
  }

  /**
   * @return If true, a notification will also be sent by email in case the webhook request fails all the retries.  The email notification will be sent to the requesting user or their organization’s admin if part of one.
   */
  @JsonProperty("webhook_failure_notification")
  public Optional<Boolean> getWebhookFailureNotification() {
    return webhookFailureNotification;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof PdfConvertToJpgRequest && equalTo((PdfConvertToJpgRequest) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(PdfConvertToJpgRequest other) {
    return webhookAuthorization.equals(other.webhookAuthorization) && apiKey.equals(other.apiKey) && format.equals(other.format) && fileId.equals(other.fileId) && destroy.equals(other.destroy) && output.equals(other.output) && quality.equals(other.quality) && pages.equals(other.pages) && resolution.equals(other.resolution) && imageSmoothing.equals(other.imageSmoothing) && profile.equals(other.profile) && webhookUrl.equals(other.webhookUrl) && webhookFailureNotification.equals(other.webhookFailureNotification);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.webhookAuthorization, this.apiKey, this.format, this.fileId, this.destroy, this.output, this.quality, this.pages, this.resolution, this.imageSmoothing, this.profile, this.webhookUrl, this.webhookFailureNotification);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static ApiKeyStage builder() {
    return new Builder();
  }

  public interface ApiKeyStage {
    /**
     * <p>Your API key</p>
     */
    _FinalStage apiKey(@NotNull String apiKey);

    Builder from(PdfConvertToJpgRequest other);
  }

  public interface _FinalStage {
    PdfConvertToJpgRequest build();

    _FinalStage additionalProperty(String key, Object value);

    _FinalStage additionalProperties(Map<String, Object> additionalProperties);

    /**
     * <p>Optional custom header for webhook requests. Format: <code>Key:Value</code> (e.g., <code>Authorization:Bearer token123</code>). This will be sent as an HTTP header in the webhook call.</p>
     */
    _FinalStage webhookAuthorization(Optional<String> webhookAuthorization);

    _FinalStage webhookAuthorization(String webhookAuthorization);

    /**
     * <p>Specifies the desired format for the API response. Choose 'json' for a JSON object or 'xml' for an XML structure.</p>
     */
    _FinalStage format(Optional<PdfConvertToJpgRequestFormat> format);

    _FinalStage format(PdfConvertToJpgRequestFormat format);

    /**
     * <p>The unique ID of a PDF file already uploaded to the API Freaks server. Use this as an alternative to uploading a new file directly.</p>
     */
    _FinalStage fileId(Optional<String> fileId);

    _FinalStage fileId(String fileId);

    /**
     * <p>If set to <code>true</code>, the input file(s) will be permanently deleted from the server immediately after the output PDF is generated.</p>
     */
    _FinalStage destroy(Optional<Boolean> destroy);

    _FinalStage destroy(Boolean destroy);

    /**
     * <p>The desired name for the output unrestricted PDF file. If not provided, a default name will be assigned.</p>
     */
    _FinalStage output(Optional<String> output);

    _FinalStage output(String output);

    /**
     * <p>Controls JPG compression quality. Higher values yield sharper images with larger file sizes.</p>
     */
    _FinalStage quality(Optional<Integer> quality);

    _FinalStage quality(Integer quality);

    /**
     * <p>Specifies the pages or ranges at which to split the PDF. Accepts individual page numbers (e.g., '1') and/or page ranges (e.g., '4-2', 'last'). Ranges can be ascending or descending. Use commas to separate entries and hyphens for ranges. Alternatively, provide only one of the following keywords: 'even' (split at every even-numbered page), 'odd' (split at every odd-numbered page), 'last' (split at the last page only), or 'all' (split into single pages). Examples: '1,4-2,last', 'odd', 'all'. Mixing special keywords with specific pages/ranges is not allowed.</p>
     */
    _FinalStage pages(Optional<String> pages);

    _FinalStage pages(String pages);

    /**
     * <p>Specifies the resolution (in DPI) for the output images. Acceptable Range is from 20 to 1200.</p>
     */
    _FinalStage resolution(Optional<Integer> resolution);

    _FinalStage resolution(Integer resolution);

    /**
     * <p>Determines the smoothing options to apply during image conversion. Valid values are 'none', 'all' or a combination of 'text', 'line', and 'image' (comma-separated).If not provided, no smoothing will be applied.</p>
     */
    _FinalStage imageSmoothing(Optional<String> imageSmoothing);

    _FinalStage imageSmoothing(String imageSmoothing);

    /**
     * <p>Specifies the color profile for the output PNG images. Acceptable values: bw (1-bit black &amp; white, smallest size, no grayscale or color), gray (8-bit grayscale), rgb (24-bit RGB color, default), rgba (32-bit RGB color with alpha channel for transparency), 4-bit (4-bit indexed color, up to 16 colors, smaller size), or 8-bit (8-bit indexed color, up to 256 colors).</p>
     */
    _FinalStage profile(Optional<PdfConvertToJpgRequestProfile> profile);

    _FinalStage profile(PdfConvertToJpgRequestProfile profile);

    /**
     * <p>The URL to which the webhook notification will be sent after the task is completed.</p>
     */
    _FinalStage webhookUrl(Optional<String> webhookUrl);

    _FinalStage webhookUrl(String webhookUrl);

    /**
     * <p>If true, a notification will also be sent by email in case the webhook request fails all the retries.  The email notification will be sent to the requesting user or their organization’s admin if part of one.</p>
     */
    _FinalStage webhookFailureNotification(Optional<Boolean> webhookFailureNotification);

    _FinalStage webhookFailureNotification(Boolean webhookFailureNotification);
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder implements ApiKeyStage, _FinalStage {
    private String apiKey;

    private Optional<Boolean> webhookFailureNotification = Optional.empty();

    private Optional<String> webhookUrl = Optional.empty();

    private Optional<PdfConvertToJpgRequestProfile> profile = Optional.empty();

    private Optional<String> imageSmoothing = Optional.empty();

    private Optional<Integer> resolution = Optional.empty();

    private Optional<String> pages = Optional.empty();

    private Optional<Integer> quality = Optional.empty();

    private Optional<String> output = Optional.empty();

    private Optional<Boolean> destroy = Optional.empty();

    private Optional<String> fileId = Optional.empty();

    private Optional<PdfConvertToJpgRequestFormat> format = Optional.empty();

    private Optional<String> webhookAuthorization = Optional.empty();

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    @java.lang.Override
    public Builder from(PdfConvertToJpgRequest other) {
      webhookAuthorization(other.getWebhookAuthorization());
      apiKey(other.getApiKey());
      format(other.getFormat());
      fileId(other.getFileId());
      destroy(other.getDestroy());
      output(other.getOutput());
      quality(other.getQuality());
      pages(other.getPages());
      resolution(other.getResolution());
      imageSmoothing(other.getImageSmoothing());
      profile(other.getProfile());
      webhookUrl(other.getWebhookUrl());
      webhookFailureNotification(other.getWebhookFailureNotification());
      return this;
    }

    /**
     * <p>Your API key</p>
     * <p>Your API key</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("apiKey")
    public _FinalStage apiKey(@NotNull String apiKey) {
      this.apiKey = Objects.requireNonNull(apiKey, "apiKey must not be null");
      return this;
    }

    /**
     * <p>If true, a notification will also be sent by email in case the webhook request fails all the retries.  The email notification will be sent to the requesting user or their organization’s admin if part of one.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage webhookFailureNotification(Boolean webhookFailureNotification) {
      this.webhookFailureNotification = Optional.ofNullable(webhookFailureNotification);
      return this;
    }

    /**
     * <p>If true, a notification will also be sent by email in case the webhook request fails all the retries.  The email notification will be sent to the requesting user or their organization’s admin if part of one.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "webhook_failure_notification",
        nulls = Nulls.SKIP
    )
    public _FinalStage webhookFailureNotification(Optional<Boolean> webhookFailureNotification) {
      this.webhookFailureNotification = webhookFailureNotification;
      return this;
    }

    /**
     * <p>The URL to which the webhook notification will be sent after the task is completed.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage webhookUrl(String webhookUrl) {
      this.webhookUrl = Optional.ofNullable(webhookUrl);
      return this;
    }

    /**
     * <p>The URL to which the webhook notification will be sent after the task is completed.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "webhook_url",
        nulls = Nulls.SKIP
    )
    public _FinalStage webhookUrl(Optional<String> webhookUrl) {
      this.webhookUrl = webhookUrl;
      return this;
    }

    /**
     * <p>Specifies the color profile for the output PNG images. Acceptable values: bw (1-bit black &amp; white, smallest size, no grayscale or color), gray (8-bit grayscale), rgb (24-bit RGB color, default), rgba (32-bit RGB color with alpha channel for transparency), 4-bit (4-bit indexed color, up to 16 colors, smaller size), or 8-bit (8-bit indexed color, up to 256 colors).</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage profile(PdfConvertToJpgRequestProfile profile) {
      this.profile = Optional.ofNullable(profile);
      return this;
    }

    /**
     * <p>Specifies the color profile for the output PNG images. Acceptable values: bw (1-bit black &amp; white, smallest size, no grayscale or color), gray (8-bit grayscale), rgb (24-bit RGB color, default), rgba (32-bit RGB color with alpha channel for transparency), 4-bit (4-bit indexed color, up to 16 colors, smaller size), or 8-bit (8-bit indexed color, up to 256 colors).</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "profile",
        nulls = Nulls.SKIP
    )
    public _FinalStage profile(Optional<PdfConvertToJpgRequestProfile> profile) {
      this.profile = profile;
      return this;
    }

    /**
     * <p>Determines the smoothing options to apply during image conversion. Valid values are 'none', 'all' or a combination of 'text', 'line', and 'image' (comma-separated).If not provided, no smoothing will be applied.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage imageSmoothing(String imageSmoothing) {
      this.imageSmoothing = Optional.ofNullable(imageSmoothing);
      return this;
    }

    /**
     * <p>Determines the smoothing options to apply during image conversion. Valid values are 'none', 'all' or a combination of 'text', 'line', and 'image' (comma-separated).If not provided, no smoothing will be applied.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "image_smoothing",
        nulls = Nulls.SKIP
    )
    public _FinalStage imageSmoothing(Optional<String> imageSmoothing) {
      this.imageSmoothing = imageSmoothing;
      return this;
    }

    /**
     * <p>Specifies the resolution (in DPI) for the output images. Acceptable Range is from 20 to 1200.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage resolution(Integer resolution) {
      this.resolution = Optional.ofNullable(resolution);
      return this;
    }

    /**
     * <p>Specifies the resolution (in DPI) for the output images. Acceptable Range is from 20 to 1200.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "resolution",
        nulls = Nulls.SKIP
    )
    public _FinalStage resolution(Optional<Integer> resolution) {
      this.resolution = resolution;
      return this;
    }

    /**
     * <p>Specifies the pages or ranges at which to split the PDF. Accepts individual page numbers (e.g., '1') and/or page ranges (e.g., '4-2', 'last'). Ranges can be ascending or descending. Use commas to separate entries and hyphens for ranges. Alternatively, provide only one of the following keywords: 'even' (split at every even-numbered page), 'odd' (split at every odd-numbered page), 'last' (split at the last page only), or 'all' (split into single pages). Examples: '1,4-2,last', 'odd', 'all'. Mixing special keywords with specific pages/ranges is not allowed.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage pages(String pages) {
      this.pages = Optional.ofNullable(pages);
      return this;
    }

    /**
     * <p>Specifies the pages or ranges at which to split the PDF. Accepts individual page numbers (e.g., '1') and/or page ranges (e.g., '4-2', 'last'). Ranges can be ascending or descending. Use commas to separate entries and hyphens for ranges. Alternatively, provide only one of the following keywords: 'even' (split at every even-numbered page), 'odd' (split at every odd-numbered page), 'last' (split at the last page only), or 'all' (split into single pages). Examples: '1,4-2,last', 'odd', 'all'. Mixing special keywords with specific pages/ranges is not allowed.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "pages",
        nulls = Nulls.SKIP
    )
    public _FinalStage pages(Optional<String> pages) {
      this.pages = pages;
      return this;
    }

    /**
     * <p>Controls JPG compression quality. Higher values yield sharper images with larger file sizes.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage quality(Integer quality) {
      this.quality = Optional.ofNullable(quality);
      return this;
    }

    /**
     * <p>Controls JPG compression quality. Higher values yield sharper images with larger file sizes.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "quality",
        nulls = Nulls.SKIP
    )
    public _FinalStage quality(Optional<Integer> quality) {
      this.quality = quality;
      return this;
    }

    /**
     * <p>The desired name for the output unrestricted PDF file. If not provided, a default name will be assigned.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage output(String output) {
      this.output = Optional.ofNullable(output);
      return this;
    }

    /**
     * <p>The desired name for the output unrestricted PDF file. If not provided, a default name will be assigned.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "output",
        nulls = Nulls.SKIP
    )
    public _FinalStage output(Optional<String> output) {
      this.output = output;
      return this;
    }

    /**
     * <p>If set to <code>true</code>, the input file(s) will be permanently deleted from the server immediately after the output PDF is generated.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage destroy(Boolean destroy) {
      this.destroy = Optional.ofNullable(destroy);
      return this;
    }

    /**
     * <p>If set to <code>true</code>, the input file(s) will be permanently deleted from the server immediately after the output PDF is generated.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "destroy",
        nulls = Nulls.SKIP
    )
    public _FinalStage destroy(Optional<Boolean> destroy) {
      this.destroy = destroy;
      return this;
    }

    /**
     * <p>The unique ID of a PDF file already uploaded to the API Freaks server. Use this as an alternative to uploading a new file directly.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage fileId(String fileId) {
      this.fileId = Optional.ofNullable(fileId);
      return this;
    }

    /**
     * <p>The unique ID of a PDF file already uploaded to the API Freaks server. Use this as an alternative to uploading a new file directly.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "file_id",
        nulls = Nulls.SKIP
    )
    public _FinalStage fileId(Optional<String> fileId) {
      this.fileId = fileId;
      return this;
    }

    /**
     * <p>Specifies the desired format for the API response. Choose 'json' for a JSON object or 'xml' for an XML structure.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage format(PdfConvertToJpgRequestFormat format) {
      this.format = Optional.ofNullable(format);
      return this;
    }

    /**
     * <p>Specifies the desired format for the API response. Choose 'json' for a JSON object or 'xml' for an XML structure.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "format",
        nulls = Nulls.SKIP
    )
    public _FinalStage format(Optional<PdfConvertToJpgRequestFormat> format) {
      this.format = format;
      return this;
    }

    /**
     * <p>Optional custom header for webhook requests. Format: <code>Key:Value</code> (e.g., <code>Authorization:Bearer token123</code>). This will be sent as an HTTP header in the webhook call.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage webhookAuthorization(String webhookAuthorization) {
      this.webhookAuthorization = Optional.ofNullable(webhookAuthorization);
      return this;
    }

    /**
     * <p>Optional custom header for webhook requests. Format: <code>Key:Value</code> (e.g., <code>Authorization:Bearer token123</code>). This will be sent as an HTTP header in the webhook call.</p>
     */
    @java.lang.Override
    public _FinalStage webhookAuthorization(Optional<String> webhookAuthorization) {
      this.webhookAuthorization = webhookAuthorization;
      return this;
    }

    @java.lang.Override
    public PdfConvertToJpgRequest build() {
      return new PdfConvertToJpgRequest(webhookAuthorization, apiKey, format, fileId, destroy, output, quality, pages, resolution, imageSmoothing, profile, webhookUrl, webhookFailureNotification, additionalProperties);
    }

    @java.lang.Override
    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    @java.lang.Override
    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
